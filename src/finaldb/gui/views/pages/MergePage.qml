import QtQuick 2.15
import FinalDB.Theme 1.0
import "../components"

// 合并去重页：表合并与去重配置
PagePlaceholder {
    theme: Theme
    badge: "合"
    pageTitle: "合并去重"
    description: "纵向/横向合并、全行/按键/模糊去重（P4 实现）"
}
